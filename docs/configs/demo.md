# configs/demo.yaml

This configuration explicitly enables a scripted demonstration with a fictional
company, a synthetic category and a synthetic evidence pair. It requires no
dataset, encoder or API key. Demo scores are configured illustrative values,
not model predictions or evaluation results. It cannot be passed to real-data
evaluation. The actual Comcast taxonomy remains empty until human review.
